import React, { FC } from 'react'
import styled from '@emotion/styled'

const Padding: FC = () => {
  return <StyledPadding></StyledPadding>
}

export default Padding

const StyledPadding = styled.div({
  paddingBottom: '40px',
})
